@model GerenciamentoPlantao.Models.ViewModels.EditarSetorViewModel

@{
    ViewData["Title"] = "Editar Setor";
}

<div class="container mt-4">

    <div class="card shadow-sm">

        <div class="card-header bg-primary text-white">
            <h3 class="mb-0">
                <i class="bi bi-building"></i>
                Editar Setor
            </h3>
        </div>

        <div class="card-body">

            <form asp-action="Edit">

                <div asp-validation-summary="ModelOnly"
                     class="alert alert-danger">
                </div>

                <input asp-for="Id" type="hidden" />

                <div class="mb-3">

                    <label asp-for="Nome"
                           class="form-label fw-bold">
                    </label>

                    <input asp-for="Nome"
                           class="form-control form-control-lg" />

                    <span asp-validation-for="Nome"
                          class="text-danger">
                    </span>

                </div>

                <div class="mb-3">

                    <label asp-for="EstabelecimentoId"
                           class="form-label fw-bold">
                        Estabelecimento
                    </label>

                    <select asp-for="EstabelecimentoId"
                            asp-items="Model.Estabelecimentos"
                            class="form-select">

                        <option value="">
                            Selecione um estabelecimento
                        </option>

                    </select>

                    <span asp-validation-for="EstabelecimentoId"
                          class="text-danger">
                    </span>

                </div>

                <div class="mb-3">

                    @if (Model.Ativo)
                    {
                        <span class="badge bg-success fs-6">
                            <i class="bi bi-check-circle-fill"></i>
                            Ativo
                        </span>
                    }
                    else
                    {
                        <span class="badge bg-danger fs-6">
                            <i class="bi bi-x-circle-fill"></i>
                            Inativo
                        </span>
                    }

                </div>

                <div class="d-flex gap-2">

                    <button type="submit"
                            class="btn btn-primary">

                        <i class="bi bi-floppy-fill"></i>
                        Salvar

                    </button>

                    @if (Model.Ativo)
                    {
                        <button asp-action="Inativar"
                                asp-route-id="@Model.Id"
                                formmethod="post"
                                class="btn btn-danger">

                            <i class="bi bi-ban-fill"></i>
                            Inativar

                        </button>
                    }
                    else
                    {
                        <button asp-action="Ativar"
                                asp-route-id="@Model.Id"
                                formmethod="post"
                                class="btn btn-success">

                            <i class="bi bi-check-circle-fill"></i>
                            Ativar

                        </button>
                    }

                    <a asp-action="Index"
                       class="btn btn-outline-secondary">

                        <i class="bi bi-arrow-left"></i>
                        Voltar

                    </a>

                </div>

            </form>

        </div>

    </div>

</div>

@section Scripts
{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}